/**
 * LeetCode - ${problemName}
 * ${problemUrl}
 */
 
package ${packageName}.${problemName};

import java.util.*;
import ${utilPackage}.*;
import lombok.extern.slf4j.Slf4j;

@Slf4j
class Solution2 implements Solution {
    
    @Override
    public ${problemSignature?no_esc} {
        
    }
}
// 时间复杂度：
// 空间复杂度：